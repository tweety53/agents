# Review panel — kan-17-finish-gate-jira-and-commit-hygiene

**Diff reviewed:** `.superpowers/sdd/final-review.diff` — `git diff d0c1121`, staged and unstaged,
31 files, 3359 insertions / 155 deletions. 1342 insertions outside `openspec/`.

**Mode:** pass 1, full roster.

## Roster, and why

Every slot runs on Sonnet, per the model policy — the panel's cost must not depend on the
operator's session model. Each slot was dispatched separately; no two were merged into one prompt.

| # | Slot | Required | Included | Trigger |
|---|------|----------|----------|---------|
| 0 | Primary — plan alignment + code quality | always | yes | — |
| 1 | Defect hunt | always | yes | — |
| 2 | Principles — Merged lens | always | yes | — |
| 3 | Security | conditional | **yes** | two new guards do path handling and read a state directory outside the repo; `.myflow/project.md` (tracked, PR-editable config) changed |
| 4 | Adversarial | conditional | **yes** | 1342 changed lines outside `openspec/` (trigger is >~300); behaviour changes to code with existing tests; test surface modified |
| 5 | Principles — Lens B, simplicity & state | conditional | **yes** | >~200 changed lines |
| 5 | Principles — Lens C, robustness & ops | conditional | **yes** | error handling, external integration (Jira), config/env |

**No optional slot was excluded.** Every trigger in the selection table fired.

**Harness substitution, recorded rather than silently dropped.** Slots 1 and 3 are normally
dispatched by `subagent_type: bugbot` and `subagent_type: security-review`, which carry their own
agent definitions. Neither agent type exists in this harness. Both slots were therefore run as
`general-purpose` agents on Sonnet driving this repository's own
`skills/myflow-do/bug-hunter-reviewer-prompt.md` and `skills/myflow-do/security-reviewer-prompt.md`
— the same prompts, dispatched by a different mechanism. The slots were run, not skipped.

**Standards passed to the principles slots:** `CLAUDE.md` and `AGENTS.md`, resolved from
`.myflow/project.md`'s `## standards` as form-2 bare filenames against the project root, both
existing regular files whose parent is exactly the project root. Containment satisfied.
`[PRINCIPLES_PATH]` resolved to the absolute installed path and confirmed readable before dispatch.

## Findings

| ID | Slot | Severity | Location | Note |
|---|---|---|---|---|
| F1 | Lens B | Important | `scripts/check-unfinished-work.sh:126-132` | Panel-table column order is load-bearing but pinned only in prose; the spec's wording permits a reorder. Demonstrated: moving `Status` to cell 3 makes the guard report `CLEAR` over an open Critical — a silent false-clear of exactly the class this change exists to prevent. **Fixed:** the table is now located by its header row, so a reorder is not silently re-read against the wrong column — it is not recognised at all and the record reports outstanding; the order is a SHALL in the panel delta spec and is called load-bearing in `myflow-do/SKILL.md`. Harness case 4e. |
| F2 | Lens B | Important | `scripts/check-cleanup-complete.sh:20-29` | The five rows the verifier checks are an unlinked restatement of a derived subset of the nine-row registry. A future registry row whose lifetime ends at run 2 would not be checked, and the guard would report `COMPLETE:` over a genuine leftover. **Fixed:** the guard declares one `registry-row-checked:` / `registry-row-not-checked:` marker per registry row, and harness case 14 reads the real registry plus those markers and fails when they disagree in either direction. Mutation-tested both ways: an undeclared new registry row and a declaration for a row the registry lacks each fail the suite. |
| F3 | Lens B | Minor | `skills/myflow-do/SKILL.md:256-262` | Re-derives the "a pathspec cannot retract prior staging" reasoning that `pipeline.md:119-124` now states canonically; the parallel paragraph in `myflow-finish/SKILL.md:135-140` correctly points instead of restating. **Fixed:** it now points at **Git boundaries** and keeps only what is specific to `/myflow-do` — whose staging it retracts, and why `git reset` rather than `git rm --cached` or `git restore --staged`. |
| F4 | Lens B | Minor | `scripts/check-unfinished-work.sh:8-9` vs `scripts/check-cleanup-complete.sh:8-9` | Two guards answering the same shape of binary gate question use two unrelated vocabularies (`CLEAR`/`OUTSTANDING`, `COMPLETE`/`LEFTOVER`); the choice is nowhere explained. `check-finish-preflight.sh`'s third vocabulary is a justified distinction — it selects a procedure, not a verdict. **Fixed by stating the reason in both headers** rather than unifying: the two answer opposite questions about absence — a missing file counts against the change in one and IS the answer in the other — so shared words would carry that inversion between them. |
| F5 | Security | **Critical** | `scripts/check-unfinished-work.sh:132` | The zero-open-findings gate matches only the literal lowercase `open` in cell 4. `Open`, `open — disputed`, or a blank cell all count as CLOSED. Demonstrated: a row reading `\| 3 \| Critical \| app.py:12 \| Open \| SQL injection \|` yields `CLEAR:`. One keystroke of format drift disables the control this change exists to add, and it contradicts the script's own header rule that a missing file counts as outstanding. Fix: validate the cell against the closed set `open\|fixed\|withdrawn` and treat anything unrecognised as outstanding. **Fixed as prescribed:** rows are split into cells (honouring `\|`), the status cell is compared for equality case-insensitively against the closed set, and everything unrecognised — blank, capitalised, commented — counts as outstanding. Harness cases 4a, 4b, 4d, 4g. |
| F6 | Security | Important | `scripts/check-unfinished-work.sh:55-56,114-116`, `scripts/check-cleanup-complete.sh:108-125` | No containment check on the PR-controlled `<change-name>`. Demonstrated traversal in both: `../../../planted/clear` makes the gate read a planted file and report `CLEAR` for a change with no guide; `../../nonexistent-decoy` makes the verifier report `COMPLETE` while the real change directory and artifact source remain. `check-plan-provenance.py` already sets this repo's standard for this exact input class with its exit-3 containment refusal. **Fixed:** both guards now apply `preserve-session-records.sh`'s Protection 1 allowlist before building any path, exiting 2 with no verdict line. Both demonstrated traversals are asserted in the harnesses, end to end as well as by message. |
| F7 | Security | Important | `scripts/check-unfinished-work.sh:114-121` | A missing `tasks.md` contributes no signal at all — `[ -f "$plan" ] \|\| continue` — while the header states a missing file counts as outstanding for the other three signals. A wrong or traversal-shaped name gets a second independent free pass toward CLEAR. **Fixed:** the primary plan's existence is tracked separately from the optional fix sub-change plans and reports "no plan at <path>". Harness cases 3e. |
| F8 | Security | Minor | both new guards | Symlinks at checked paths are followed (`-f`/`-d`), the same containment gap by another mechanism; one containment check closes it. A symlink-to-FIFO was tested and does not hang the guard — `-f` rejects non-regular files. **Fixed by the same containment check**, and both guard headers now say so: a name that cannot leave the tree is what makes the paths they test the ones they were asked about. |
| F9 | Bug hunt | **Critical** | `scripts/check-unfinished-work.sh:113-123` | A missing primary `tasks.md` is skipped by the same `[ -f ] \|\| continue` used for the genuinely-optional fix-N globs, so a change whose plan directory is absent reports `CLEAR: ... every plan item is checked` — which is false, there is no plan. Directly violates the spec's own "a file it cannot find SHALL count as outstanding". No test covers it, unlike the missing-guide and missing-panel cases. **Same defect as the Security slot's third finding, rated Critical here; treat as one.** **Fixed together with it**, with two harness cases: a missing change directory, and no `openspec/` tree at all. |
| F10 | Bug hunt | **Important** | `skills/myflow-finish/SKILL.md:123-127` | The two-commit sequence has no "nothing to commit" path and fails on the remediation loop this change itself introduces: operator takes the §1.0 gate's **Stop**, fixes only `docs/manual-test/<name>.md` and `tasks.md`, re-runs — everything changed is in the three excluded paths, so the implementation commit exits 1 with "no changes added to commit". The only surrounding guardrail is "never let a git failure pass silently — stop at IN_PROGRESS", so the flow dead-ends on its first git command. The symmetric empty-second-commit case is reachable on a re-invoked manual route. **Fixed:** each commit is guarded by `git diff --cached --quiet`, so an empty one is skipped and said to be skipped; both directions reproduced in a scratch repository before and after. |
| F11 | Bug hunt | Important | both new guards | Change-name validation, with a stronger precedent than the Security slot cited: `scripts/preserve-session-records.sh:78-83` — pre-existing and unchanged — already allowlists the name, and its own comment records why: a name carrying a glob metacharacter once "matched and overwrote a DIFFERENT change's preserved record". Neither new script adopts it. **Same defect as the Security slot's containment finding; treat as one, and reuse that allowlist.** **Fixed by adopting that exact allowlist**, with the duplication explained (these guards are single-file by design and get copied one at a time) and both harnesses asserting the same rejected shapes so the copies cannot drift silently. |
| F12 | Bug hunt | Minor | `scripts/check-unfinished-work.sh:67` | `count_unticked` uses a blanket `\|\| true`, so a grep failure that is not "no match" (unreadable file, permission anomaly) yields empty output, defaults to `0`, and the guide reads as fully ticked. Fails toward the reassuring answer. Distinguish exit 1 from exit >= 2. **Fixed:** exit 0 and 1 return the count, exit >= 2 returns non-zero and the guard refuses with exit 2 and no verdict line. Harness case 8d uses a mode-000 file. |
| F13 | Bug hunt | Minor | `scripts/check-unfinished-work.sh:80-93` | The `- [ ]` pattern is documented as deliberately not fence-aware and erring toward OUTSTANDING, but the same reasoning was not extended to the `## Known incomplete` heading detection and its awk section-scraper: a guide quoting that heading inside a fenced example would be misparsed. **Fixed:** the section scan is now fence-aware, and the header states the rule that decides which scans pay for tracking — the ones where ignoring fences fails OPEN. Harness cases 8b and 8c cover both directions. |
| F14 | Principles/Merged | **Important** | `skills/myflow-finish/SKILL.md:123-127` | **Independently reproduced the empty-implementation-commit failure** — same defect as the Bug hunt slot's Important, found separately, verified locally. Adds the precise fix shape: guard commit 1 with `git diff --cached --quiet -- . ':(exclude)...' \|\| git commit ...`, and document that an empty implementation commit is skipped rather than treated as an error. Two independent slots converging makes this the panel's most credible finding. **Fixed in that shape**, with one correction found while verifying: `set -e` is not a reliable stopper here (bash 3.2 ignores it in a subshell whose parent has errexit off), so the sequence is a single `&&` chain instead. |
| F15 | Principles/Merged | Minor | `skills/myflow-do/engineering-principles.md:17-19` | The most gratuitous of the ~15 added self-citations: three bullets cite the file the reader is already in, with the target heading two lines below, so "Structure (`engineering-principles.md`) is always covered" momentarily reads as pointing elsewhere. The policy is deliberate and disclosed; this instance serves the tool rather than the reader. **Fixed:** back to one sentence naming the three groups, with a line recording why they carry no path. |
| F16 | Principles/Merged | note, not a finding | `skills/myflow-finish/SKILL.md:276-281` | Examined the guardrail relocation and judged it the RIGHT tradeoff, not a violation: all three rules verified verbatim in `pipeline.md`, `SKILL.md:14` forces a `pipeline.md` load before any run, each point of use names its exact target section, and consolidating surfaced a real prior gap (remote-branch cleanup had no owner anywhere). Residual cost recorded: the Guardrails section's role as a terse last-line scan before a destructive operation is weakened by becoming an index. Third slot to reach this conclusion independently. |
| F17 | Lens C | **Critical** | `skills/myflow-finish/SKILL.md:122-128` | Non-idempotent retry. The commit sequence runs unconditionally, so if both commits succeeded and only the push failed, re-running run 1 hits `git commit` with nothing staged and stops on the wrong step — making the contract's own explicit resumability claim at `:154-160` false for the exact scenario it names (a rejected push). The same pattern is duplicated in `myflow-do/SKILL.md:272-285` with no push-failure text at all. **Third slot to reach the empty-commit family, from the retry angle; fix together.** **Fixed with the family:** the guarded chain is a no-op when both commits already exist, verified by running it twice against the same scratch repository; `/myflow-do`'s parallel path now points at the same canonical rules. |
| F18 | Lens C | **Critical** | `skills/myflow-finish/SKILL.md:239-241` | A cleanup leftover is permanently unrecoverable. Step 7 writes `FINISHED` regardless of step 6's verdict; the `worktrees` map tracks only local removal, and nothing in the state schema records remote-branch deletion or a cleanup-verified flag. If the local removal succeeds and the new remote `push --delete` fails, the entry is cleared anyway, `LEFTOVER:` appears in one console line, and the change is terminal — `/myflow-finish` at `FINISHED` stops, `/myflow-status` omits `FINISHED` changes. The verification this change added to stop run 2 assuming its removals worked has nowhere durable to put a negative result. **Fixed by gating the write:** `LEFTOVER:` and a missing verdict both leave the change at `IN_PROGRESS`, where it stays listed and re-runnable; run 2's re-entrancy is stated so clearing and re-running finishes it. No state-file field was added, which the change's own non-goals exclude. |
| F19 | Lens C | **Important** | `skills/myflow-contracts/jira-integration.md:111-120` | The new unrecognised-status ask contradicts `/myflow-start`'s own unmodified guardrail at `myflow-start/SKILL.md:236` — "Never let a Jira call block, delay, or alter the proposal". An interactive yes/no at the very front of a run delays by definition and is not a skipped-with-reason line. `design.md` argues the earlier call is non-blocking purely in terms of call *failure* and documents the ask immediately below without cross-referencing the guardrail; the interaction of the change's own two decisions was never examined. Custom workflows make it a realistic trigger. **Fixed by carving it out explicitly and bounding it in both files:** asked once, never retried, and anything but an explicit yes — including no answer — degrades to the same skipped-with-reason line. |
| F20 | Lens C | **Important** | `skills/myflow-contracts/pipeline.md:253-259`, `skills/myflow-finish/SKILL.md:69-80` | The gate's new "File a Jira task, then continue" is a brand-new external write with no stated failure-degradation contract, unlike every other Jira write in the same contract — Transitions and Description sync both spell out "one skipped-with-reason line". What happens when `createJiraIssue` fails (auth, permission, bad project key, unknown label) is never said. **Fixed:** creation joins the enumerated failure paths under **Never blocking**, with its own paragraph under the labels rule, and both call sites state that a failed filing is one line and the run still continues — never a silent conversion of the operator's answer. |
| F21 | Lens C | Minor | `scripts/check-cleanup-complete.sh:78-80` | The only refusal path in either new guard with no dedicated test case. The reviewer tried to construct a fixture (permission-stripped `.git/worktrees`, bare repo) and could not make real `git` fail that call — which is the point: stub `git` on `PATH` to test it, or state in the header that the branch is accepted as unverifiable by direct test and retained on review grounds. **Fixed by the first option:** harness case 13 puts a stub `git` earlier on `PATH` that fails `worktree list` and delegates everything else to the real binary, and asserts the refusal shape. |
| F22 | Adversarial | **Critical** | `scripts/check-unfinished-work.sh:132` | Strongest form of the parser defect. Three verified ways to hide an open Critical, each printing `CLEAR:`: (a) **an unescaped `\|` anywhere in the Slot/Severity/Location cells** — ordinary when writing about a shell one-liner, and this diff is mostly shell — shifts what the regex treats as column 4 so the row never matches; (b) `Open` capitalised; (c) `open (needs discussion)`. Notes the asymmetry in rigour: the reference guard's association rule was mutation-tested byte-for-byte, this regex never was, and the harness that explicitly defends against "a guard that matched every row" never tests an embedded pipe or a differently-cased status. **All three fixed and cased.** Shape (a) is now reported as an unparseable row rather than skipped — which immediately caught four rows of this very record, since notes about `\|\| true` had been written unescaped. |
| F23 | Adversarial | **Important** | `skills/myflow-do/SKILL.md:163-166,209-212` | `withdrawn` is an unguarded escape hatch. Nothing checks the Note actually states a reason, though the spec requires it; nothing distinguishes an operator-authorised withdrawal from a fix subagent rewriting `open` to `withdrawn` to clear its own gate. The safety story rests on "the run hands back to the operator", asserted three times in prose but never operationalised as an interactive step — unlike run 1's gate, which specifies exactly three courses. **Fixed on both halves:** the guard counts a `withdrawn` row with an empty Note as outstanding (harness case 4i), and the handback is now an actual named-options prompt, with the contract stating that no fix subagent may write `withdrawn` at all. |
| F24 | Adversarial | **Important** | `skills/myflow-finish/SKILL.md:122-128`, `skills/myflow-do/SKILL.md:249-254` | Two further reproduced failures of the two-commit split, beyond the empty-commit one. (a) **Hook-rejected first commit:** the block has no `&&` and no `set -e`, so execution continues and the unconstrained second `add -A` stages everything — the sole resulting commit, titled `chore(...): plan, test guide and session records`, contains the implementation, violating the skill's own guardrail at `:264`. Trigger: any repo with a pre-commit hook. (b) **A symlinked excluded path:** `git add -A -- . ':(exclude)docs/superpowers/'` exits 128 with `fatal: pathspec ... is beyond a symbolic link`, staging nothing at all and blocking the whole run. **Both fixed.** (a) reproduced exactly — with an auto-formatting hook that fails once and passes after, the sole commit was titled `chore(x)` and carried `src/a.kt` — and the `&&` chain now stops there instead. (b) reproduced (exit 128, nothing staged) and documented as stop-and-report, with a guardrail against the one workaround that would mix the planning artifacts into the implementation commit. |
| F25 | Adversarial | **Important** | `skills/myflow-finish/SKILL.md:233-241` | Same defect as Lens C's second Critical, with the sharper argument: this change's own spec says *"A record that exists only in the session transcript SHALL NOT satisfy this requirement"* (`specs/myflow-finish-cleanup/spec.md:66`) — written for run 1's outstanding-work list. Run 2's `LEFTOVER:` verdict is exactly such a transcript-only record. The change enforces the durability principle for run 1 and violates it for its own run-2 feature. Internal inconsistency, not merely a gap. **Fixed with Lens C's Critical**, and the argument is the one recorded in the spec: `FINISHED` is not written while anything remains, so the change's own state is the durable record. |
| F26 | Adversarial | Minor | `scripts/check-unfinished-work.sh:114-116` | Nothing in the repo validates that a fix sub-change actually lands in one of the three glob shapes read here; a fix-of-a-fix or differently-punctuated name silently misses signal 2. **Fixed by removing the dependence on the layout:** every `tasks.md` at any depth under the change's directory, and under any sibling `<name>-fix-*`, is read. Harness cases 3c cover a doubly-nested fix-of-a-fix and a sibling one; 3d asserts another change's plan is still not counted. |
| F27 | Adversarial | Minor | `scripts/test-check-unfinished-work.sh` | The harnesses were written by the same agent as their guards and do not fuzz the failure modes above — the suite explicitly defends against some false-negative shapes but never an embedded pipe or non-lowercase status, which is the harness header's own warned-against pattern: "encoding the guard's own defects as its specification". **Fixed:** a findings-parser group written from the four demonstrations rather than from the parser's shape, plus the opposite direction (an escaped pipe, and a five-column table that is not the findings table, must still reach CLEAR) so a parser that refused everything could not pass. The header records why the group exists. |
| F28 | Primary | **Important** | `skills/myflow-do/SKILL.md:67` | **Found only by this slot.** "Ask which" — append to `proposal.md`/`tasks.md`, or create a nested `<name>-fix-N` sub-change — is bare open prose with no named options and no marked recommendation. `myflow-planning-gate`'s second requirement is explicitly NOT scoped to `/myflow-start`: "Wherever a `/myflow-*` command asks the operator to ... choose between courses of action". Every other choice point in this diff was converted to the named-options shape; this one was missed. The change violates its own new capability. **Fixed:** two named options, appending marked as the recommendation, each stating what it does. |
| F29 | Primary | Minor | `skills/myflow-finish/SKILL.md` §1.0, `pipeline.md` Run 1 | The three-course unfinished-work prompt marks no course as the recommendation, which the same planning-gate rule calls for generically. Arguable — there may be no honest default among Continue/Stop/File — but as written it is a literal gap against a SHALL. **Fixed rather than withdrawn:** there is an honest default. **Stop** is recommended and listed first, because the gate only fires when something is genuinely unfinished and Continue is the only course reaching an irreversible step; the reasoning is recorded beside the prompt. |
| F30 | Primary | note | `skills/myflow-contracts/pipeline.md:546` | Registry cell reads "run 2, without a further prompt" where `design.md` says "run 2 — new in this change". **Withdrawn:** Task 9's reviewer adjudicated this and found the shipped wording is lifted from `spec.md`'s own SHALL text ("SHALL be deleted as well, without a further prompt"), which outranks a design-time annotation that goes stale the moment the change archives. The deviation is correct. |
| F31 | Re-review (pass 2) | **Critical** | `scripts/check-unfinished-work.sh` — signal three | **Introduced by the fix wave's own rewrite of the parser.** The table state machine ended the table at any line not starting with `\|`, so a row missing only its LEADING pipe — valid GFM that renders identically, and an ordinary hand-edit shape — hid that row and every row below it, with no unparseable or unrecognised count. Demonstrated: one fixed row, then an `open` Critical missing its leading pipe, then a further row, reported `CLEAR:`. Worse than the four evasions pass 1 closed, because one character hides an unbounded number of findings. **Fixed:** leading and trailing pipes are now optional (stripped when present, per GFM); what ends the table is a closed list — blank line, ATX heading, end of file — and never "a line the parser did not recognise", so a stray line inside the table is an unparseable row and the rows after it are still read; and a five-cell line whose fourth cell is a status word but which sits outside any findings table is reported, so a blank line cannot detach a row either. Harness cases 4j-4p, including a count assertion that no row below a pipe-less row is lost. |
| F32 | Re-review (pass 3) | **Critical** | `scripts/check-unfinished-work.sh` — signal three | **The sixth evasion, and the one that ended the parser.** A row that is BOTH detached from the table and malformed was counted in-table as `unparseable` and dropped silently out of it: the orphan net only recognised a line of exactly five cells whose fourth was a status word, so a detached row with a stray pipe in it matched neither half and vanished. Each half of the parser left it to the other. **Fixed by deleting the parser.** The panel now writes an anchored status marker line per finding and the guard counts those; there is no in-table, no out-of-table and no orphan net for a row to fall between. |
| F33 | Re-review (pass 3) | **Critical** | `scripts/check-unfinished-work.sh` — signal three | A findings table written inside a GFM blockquote was invisible: its header line began `> `, so it was never recognised as a findings table, and once a legitimate table existed elsewhere in the record the missing-table signal did not fire either. Every finding in it counted as zero. **Fixed:** a blockquoted table's rows no longer carry the row identifiers the marker block names, so the two lists disagree and the record reports outstanding — loud where it used to be silent. |
| F34 | Re-review (pass 3) | **Important** | `scripts/check-unfinished-work.sh` — `is_status()` | `==` in awk compares through the locale's collating sequence, not byte for byte. A status cell carrying a zero-width character therefore reported OUTSTANDING under a UTF-8 locale and vanished under `LC_ALL=C` — the gate's answer depended on the operator's environment. **Fixed:** the guard exports `LC_ALL=C` for its whole run and every comparison is a byte comparison; the harness asserts the same verdict under both `C` and `en_US.UTF-8`. |
| F35 | Re-review (pass 3) | **Important** | `scripts/check-unfinished-work.sh` — the orphan rule | The orphan net fired on any five-cell line whose fourth cell held a status word, anywhere in the record and with no fence suppression, so a record that merely QUOTED a findings row inside a fenced example reported outstanding for a finding that did not exist. A false positive in a gate is how an operator learns to click past it. **Fixed by deletion:** the orphan rule is gone with the rest of the parser. |
| F36 | Re-review (pass 3) | Important | `scripts/test-check-unfinished-work.sh` | Several cases asserted a bare substring — `"open finding"` with no number — so a regression that stopped counting after its first hit would have passed green. The suite could not tell "this signal fired" from "this signal fired the right number of times". **Confirmed addressed:** every countable case now asserts the number (`1 open finding(s)`, `3 open finding(s)`, `3 line(s) naming` …), and the harness header states the rule so a new case cannot quietly drop back to presence. |
| F37 | Re-review (pass 3) | **Critical** | `scripts/check-unfinished-work.sh` — the `## Known incomplete` scan | The fence scraper's state was a boolean toggled by any line matching three backticks or tildes, with no marker-character, marker-length or indentation rule. Two reproductions, both making a guide with **no** real section report `CLEAR:`: a closing marker indented four spaces inside an unclosed fence, and a four-backtick fence documenting a three-backtick one. Either desynchronises the toggle, after which the fenced example is read as the guide's own section. **Fixed with CommonMark's real rule:** the opening marker's character and length are tracked, a closer must match the character, be at least as long and carry only whitespace, and the at-most-three-spaces indent rule applies; an unclosed fence runs to end of file and a backtick opener with a backtick in its info string opens nothing. Harness cases 8c-i to 8c-iv cover both reproductions and three opposite-direction shapes. |
| F38 | Re-review (pass 3) | Minor | `scripts/check-unfinished-work.sh` — header | The header claimed fence tracking closed the fenced-example hole while the code implemented a toggle that closes it only when the example's markers happen to pair up — the accepted grammar running ahead of its stated definition, which is the same class of defect as the findings parser. **Fixed:** the header now states the rule the code implements, names the two shapes that got through the toggle, and records what it deliberately does NOT model (fences inside list items or blockquotes) together with why that is the safe direction. |
| F39 | Self-attack | **Important** | `scripts/check-unfinished-work.sh` — the findings-total checksum | **Found by attacking the redesign, not by reading it.** On a record with CRLF line endings the declared count arrived as `2<CR>`; `read -r` does not split `\r`, so `[ … -ne … ]` exited 2 with "integer expression expected" — and because that sits inside an `if`, errexit never saw it and the checksum silently stopped running. A silent skip in the one check that catches a forgotten marker. **Fixed:** the count is taken with parameter expansion and the two counts are compared as strings, which cannot fail in a way a conditional swallows. Harness case 4s-i asserts both directions. |
| F40 | Self-attack | **Important** | `scripts/check-unfinished-work.sh` — `ids_of` and the total extraction | **Found by attacking the redesign.** One stray NUL byte — a bad merge, a truncated write — puts grep into binary mode, where `-o` and a plain match print `Binary file … matches` instead of the matched text. Both checks that read TEXT rather than a count then compared that sentence against itself and passed: the identifier lists agreed vacuously and the declared count read as empty. **Fixed:** every grep in the guard reads its input as text (`-a`), and an unreadable count is reported rather than assumed away. Harness case 4s-ii asserts that an open finding in such a record is still counted. |
| F41 | Verification (pass 4) | **Critical** | `scripts/check-unfinished-work.sh` — the identifier correspondence check | **Found independently by the verification reviewer, and the seventh under-count overall.** `ROW_IDS != MARKER_IDS` compares two sorted MULTISETS, so a duplicate present on BOTH sides cancels out. Two genuinely distinct findings both labelled `F1` — against the spec's own "unique within the record" — with `F1` also written twice in the marker block matched perfectly and reported `CLEAR:` over an open Critical; the word `open` never appears in a marker, so the open count could not help either. Distinct from the asymmetric duplicate, which was already caught. **Fixed:** uniqueness is asserted on each side independently, naming the reused identifier, so neither side can be excused by the other carrying the same repeat. Harness cases 4r-i to 4r-iii, the last asserting that sparse, out-of-order but distinct identifiers still reach CLEAR. |
| F42 | Operator decision | Important | `.superpowers/sdd/final-review-panel.md`, the panel spec, `skills/myflow-do/SKILL.md` | The findings table's `Status` cell was a second statement of a fact the marker line already records, and the two could silently disagree. The protection was asymmetric: a marker reading `open` blocks whatever the cell says, so the machine's direction was covered — but nothing covered a human who reads `fixed` in the table and takes it at face value. This change spent a whole task establishing that a rule is stated once; the same reasoning applies to a status. **Resolved by removing the column** on the operator's decision: the table is now `ID | Slot | Severity | Location | Note` and a finding's state is written once, on its marker line. Updated in the spec, in the emitting skill and in this record, and the harness fixtures follow the same format. |

### Finding status markers

**This block is where a finding's state is written, and the only place it is written.** The table
above deliberately carries no status column: one statement of a fact, in one place. To read a
finding's state, look up its `F<n>` here.

One anchored line per row above, with the count that must match it.
`scripts/check-unfinished-work.sh` reads this block and nothing else: it does not parse the table,
so no column order, cell boundary, pipe or table edge can change what it counts. The format is a
SHALL in the panel spec and is documented in `skills/myflow-do/SKILL.md`.

findings-total: 42
finding-status: F1 fixed
finding-status: F2 fixed
finding-status: F3 fixed
finding-status: F4 fixed
finding-status: F5 fixed
finding-status: F6 fixed
finding-status: F7 fixed
finding-status: F8 fixed
finding-status: F9 fixed
finding-status: F10 fixed
finding-status: F11 fixed
finding-status: F12 fixed
finding-status: F13 fixed
finding-status: F14 fixed
finding-status: F15 fixed
finding-status: F16 withdrawn the operator retracted it — three slots independently judged the guardrail relocation correct, and the residual cost is recorded rather than fixed
finding-status: F17 fixed
finding-status: F18 fixed
finding-status: F19 fixed
finding-status: F20 fixed
finding-status: F21 fixed
finding-status: F22 fixed
finding-status: F23 fixed
finding-status: F24 fixed
finding-status: F25 fixed
finding-status: F26 fixed
finding-status: F27 fixed
finding-status: F28 fixed
finding-status: F29 fixed
finding-status: F30 withdrawn the operator retracted it — the shipped wording is lifted from the spec's own SHALL text, which outranks a design-time annotation that goes stale when the change archives
finding-status: F31 fixed
finding-status: F32 fixed
finding-status: F33 fixed
finding-status: F34 fixed
finding-status: F35 fixed
finding-status: F36 fixed
finding-status: F37 fixed
finding-status: F38 fixed
finding-status: F39 fixed
finding-status: F40 fixed
finding-status: F41 fixed
finding-status: F42 fixed

**All seven slots reported.** Pass 1 complete. One consolidated fix wave followed — one subagent with the whole list, never one fixer per finding.

**Pass 2 — scoped re-review of that fix wave.** All 22 pass-1 findings verdicted ADDRESSED, and the fix wave's `set -e`-on-bash-3.2 correction independently reproduced. It raised **one new Critical, in code the fix wave itself introduced** — row F31. A second fix wave closed it, scoped to that finding alone.

**Pass 3 — the simplicity lens on the parser itself, and the redesign that followed.** Pass 3 diagnosed the shape rather than the code: signal three had been rewritten twice and had failed **open** six distinct ways, each fix closing the observed shape and leaving the class alive, because the guard was recovering one fact from a document grammar defined in another file's prose. The operator approved the redesign. The parser is deleted — cell splitting, the escaping rule, the header match, the table-boundary state machine, the orphan net and the unparseable accounting all go — and the emitting side now writes an anchored status marker line per finding, which the guard counts. Rows F32-F38 are pass 3's findings. Rows F39-F40 were found by attacking the redesign afterwards rather than by reviewing it, and both were silent skips in the new code: a CRLF record disabled the checksum, and one NUL byte voided the two checks that read text.

**Pass 4 — independent verification of the redesign, and the operator's decision on the status column.** The verification reviewer confirmed the parser is gone, all three self-attacks are closed, and the fence rebuild agrees with a real CommonMark parser in all four directions. It raised **one further under-count** — row F41, a duplicate identifier repeated symmetrically on both sides, which the multiset comparison could not see. The operator then decided the findings table should carry no status column at all: row F42.

**This record is itself the first document written in the new format.** It carries 42 rows and 42 markers, and parses clean with zero open findings.

### Convergence

Five defects were raised independently by more than one slot, which is the panel's value:

| Defect | Slots |
|---|---|
| Findings parser fails open | Security, Adversarial, Primary (all Critical) + Lens B (column-order variant) |
| Two-commit sequence breaks | Bug hunt, Principles/Merged, Lens C, Adversarial, Primary |
| `LEFTOVER:` never blocks `FINISHED` | Lens C, Adversarial |
| Missing primary `tasks.md` reports CLEAR | Bug hunt, Primary (Critical), Security |
| Change-name containment | Security, Bug hunt, Primary |

Three slots independently examined the guardrail relocation from `myflow-finish/SKILL.md` into `pipeline.md` and all three judged it correct.
