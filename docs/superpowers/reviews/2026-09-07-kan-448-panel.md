# Review panel — kan-448

Rendered from the store. Do not edit: the findings are rows, and the next render overwrites this file.

| ID | Slot | Severity | Location | Note |
|---|---|---|---|---|
| F1 | primary | Minor | scripts/lib/post-mutation-check.sh:174-179 | the lib's documented rc=2 contract has no consumer; the guards fail open on a failed recompute |

findings-total: 1
finding-status: F1 open

reproducers-total: 1
finding-reproducer: F1 cd /Users/tweety53/Projects/agents/.worktrees/kan-448 && bash -uc '. scripts/lib/post-mutation-check.sh; snap="$(snapshot_tree_state .)"; check_tree_restored /nonexistent-worktree "$snap"; echo "lib rc=$?"; drift="$(check_tree_restored /nonexistent-worktree "$snap" || true)"; [ -n "$drift" ] && echo "caller: drift" || echo "caller: no drift -> guard passes"'
