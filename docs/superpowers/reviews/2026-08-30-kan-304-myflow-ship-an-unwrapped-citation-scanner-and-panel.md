# Review panel — kan-304-myflow-ship-an-unwrapped-citation-scanner-and

Rendered from the store. Do not edit: the findings are rows, and the next render overwrites this file.

| ID | Slot | Severity | Location | Note |
|---|---|---|---|---|
| F1 | Code review (low) | medium | skills/flow/review-panel.md:14 | review-panel.md's new citation-check step never documents what to do on check-panel-citation-trigger.sh's exit 2 (cannot answer), leaving no defined fallback, unlike the established check-visual-trigger.sh pattern which handles exit 2 explicitly |
| F2 | Bugbot | medium | scripts/check-panel-citation-trigger.sh:65 | no test case exercises a merge-base value shaped like a git option, so --end-of-options's option-injection hardening on the COMMITTED diff call is untested and can be silently dropped |
| F3 | Bugbot | medium | scripts/check-panel-citation-trigger.sh:60 | same gap as F2, on the rev-parse --verify call: its --end-of-options hardening is untested |
| F4 | Bugbot | medium | scripts/test-check-panel-citation-trigger.sh:164 | F2/F3's round-1 fix added a text-presence grep (grep -cF over the whole file) rather than a call-site-anchored check, so a decoy comment containing the same literal string satisfies the check while the real --end-of-options hardening is silently absent from both live git invocations — the exact defect class F2/F3 existed to catch is still reachable undetected |
| F5 | Bugbot | medium | scripts/test-check-panel-citation-trigger.sh:164 | round 2's comment-stripping fix (grep -v '^[[:space:]]*#') only anchors at line-start, so a trailing inline comment on the same physical line as the real (now-stripped) call still carries the literal string and satisfies the grep, leaving the same defect class reachable a third way |
| F6 | Primary | low | scripts/test-check-panel-citation-trigger.sh:174 | case 12's grep counts the literal string anywhere in comment-stripped source, never anchored to the two actual call sites, so a dead-code placement (not a comment) also satisfies it while the real hardening is absent — a fourth variant of the same recurring class |
| F7 | Bugbot | medium | scripts/check-panel-citation-trigger.sh:42 | the guard's own documented exit-2 contract for a failing git invocation on the COMMITTED/STAGED/UNSTAGED diff lookups was entirely untested — deleting all three error-handling blocks left every existing case passing |
| F8 | Primary | low | scripts/test-check-panel-citation-trigger.sh:181 | case 12's runtime trace observes the parsed source line, not what a shadowing shell function does with the arguments, so a guard deliberately rewritten to shadow git can defeat the flag's real effect while the trace-based check still passes |
| F9 | Bugbot | low | scripts/test-check-panel-citation-trigger.sh:181 | case 12's trace grep proves 'some git invocation carried the flag', not 'the specific call that computes COMMITTED/rev-parse did' — an unrelated decoy invocation with the same flag satisfies it |
| F10 | Primary | low | scripts/test-check-panel-citation-trigger.sh:181 | F9's fix does not verify the matched trace line is the real gating call — a decoy referencing $MERGEBASE (not a hardcoded value) satisfies the MERGEBASE-anchored assertion without being the call that feeds COMMITTED= |
| F11 | Primary | low | scripts/test-check-panel-citation-trigger.sh:168 | F8's git-shadow regex requires parens adjacent with no space, so the valid bash form git ( ) { ... } evades case 11b's shadow-function detection entirely |
| F12 | Bugbot | medium | scripts/check-panel-citation-trigger.sh:37 | A PATH swap scoped to the rev-parse call (shimming git to drop --end-of-options, restored before the diff calls) defeats both case 11b (no function/alias is defined, only a PATH= assignment) and case 12 (bash -x xtrace reflects source text, not the binary actually resolved and executed) simultaneously |
| F13 | Code review (low) | medium | scripts/test-check-panel-citation-trigger.sh:168 | F8's shadow-detection regex is anchored to the start of the line, so a git() shadow function placed after a leading statement (e.g. 'true; git() { ... }') on the same line evades detection entirely — a third bypass class of the same regex, distinct from F11's space-in-parens variant |
| F14 | Code review (low) | medium | scripts/test-check-panel-citation-trigger.sh:194 | the F8/F11/F13 mutation-proof's awk insertion anchored on ^GIT_BIN=, which is the OPENING line of the multi-line GIT_BIN=... \|\| { ... } statement, so the injected shadow landed inside the \|\| error branch and was never actually defined in the generated shadow copy — the proof passed for the wrong reason (no shadow existed to intercept anything), not because GIT_BIN structurally defeats a shadow |
| F15 | Bugbot | low | scripts/test-check-panel-citation-trigger.sh:183 | the F8/F11/F13 negative check (forbid bare-git shapes via regex) inherited the same unbounded-enumeration problem it replaced: a double-quoted bare "git" -C call evades the excluded-character class and reports zero bare calls while a shadow function could still intercept it. Not exploitable against the two flag-bearing call sites (case 12s slash-anchored trace independently catches those), but the checks own completeness claim was false as stated |

findings-total: 15
finding-status: F1 fixed
finding-status: F2 fixed
finding-status: F3 fixed
finding-status: F4 fixed
finding-status: F5 fixed
finding-status: F6 fixed
finding-status: F7 fixed
finding-status: F8 fixed
finding-status: F9 fixed
finding-status: F10 fixed
finding-status: F11 fixed
finding-status: F12 fixed
finding-status: F13 fixed
finding-status: F14 fixed
finding-status: F15 fixed

reproducers-total: 15
finding-reproducer: F1 none — a doc-completeness gap, not a runtime reproducer: review-panel.md's citation-check wiring branches on exit 0 and exit 1 but never documents exit 2 (usage error), unlike the sibling check-visual-trigger.sh wiring in verify-and-handoff.md which explicitly handles its own exit 2
finding-reproducer: F2 none — mutation testing, not a single runnable command: remove --end-of-options from the COMMITTED diff call in scripts/check-panel-citation-trigger.sh, rerun scripts/test-check-panel-citation-trigger.sh, observe all 12 cases still pass, then revert
finding-reproducer: F3 none — mutation testing, not a single runnable command: remove --end-of-options from the rev-parse --verify call in scripts/check-panel-citation-trigger.sh, rerun scripts/test-check-panel-citation-trigger.sh, observe all 12 cases still pass, then revert
finding-reproducer: F4 none — mutation testing, not a single runnable command: remove --end-of-options from both real git call sites in scripts/check-panel-citation-trigger.sh, but leave a comment near each containing the literal string '--end-of-options' (or even one unrelated decoy comment line anywhere in the file containing both literal strings); rerun scripts/test-check-panel-citation-trigger.sh — case 12's grep -cF checks still pass because they match ANY occurrence of the string in the file, not the real git invocation specifically
finding-reproducer: F5 none — mutation testing, not a single runnable command: strip --end-of-options from both real call sites in scripts/check-panel-citation-trigger.sh but leave a trailing inline comment on the same line as each stripped call (e.g. 'if ! git ... rev-parse --verify ...; then  # rev-parse --verify --end-of-options'); rerun scripts/test-check-panel-citation-trigger.sh — case 12 still passes because grep -v '^[[:space:]]*#' only strips whole-line comments, not trailing ones
finding-reproducer: F6 none — mutation testing: strip --end-of-options from both real call sites, add a dead non-comment line elsewhere (e.g. DEBUG_UNUSED="rev-parse --verify --end-of-options and diff --name-only --end-of-options") after set -euo pipefail; rerun scripts/test-check-panel-citation-trigger.sh — case 12 still passes because the literal string appears in genuine non-comment source, just not at the real call site
finding-reproducer: F7 none — mutation testing: replace the COMMITTED/STAGED/UNSTAGED diff calls' error handling ('|| { echo ...; exit 2; }') with '|| true' (silently swallowing a failing git diff invocation instead of exiting 2 with a message); rerun scripts/test-check-panel-citation-trigger.sh — all 12 original cases still passed
finding-reproducer: F8 none — mutation testing: define a shell function named git inside check-panel-citation-trigger.sh that strips --end-of-options from every call before delegating to command git; run scripts/test-check-panel-citation-trigger.sh — all 22 cases pass because bash -x traces the source line as written, not the shadowing function's actual behavior
finding-reproducer: F9 none — mutation testing: strip --end-of-options from the real COMMITTED call, insert an unrelated decoy git diff --name-only --end-of-options /dev/null call immediately before it; run scripts/test-check-panel-citation-trigger.sh — case 12's trace grep matches the decoy's trace line and passes, while the real call is unprotected
finding-reproducer: F10 none — mutation testing: strip --end-of-options from the COMMITTED call at scripts/check-panel-citation-trigger.sh:42, add a no-op decoy line right after the merge-base resolution check referencing $MERGEBASE (e.g. git -C "$WORKTREE" diff --name-only --end-of-options "${MERGEBASE}..HEAD" >/dev/null 2>&1 || true); rerun scripts/test-check-panel-citation-trigger.sh — case 12 still passes because the decoy's trace line carries the run's real MERGEBASE value too, and the ^\++ anchor does not distinguish which traced line is the actual COMMITTED= assignment
finding-reproducer: F11 none — mutation testing: add a shell function defined as 'git ( ) { ... strip --end-of-options ... }' (space between the parens, valid bash) to scripts/check-panel-citation-trigger.sh; rerun scripts/test-check-panel-citation-trigger.sh — case 11b's GIT_SHADOW_COUNT stays 0 because the regex git[[:space:]]*\(\) requires the parens adjacent with no space between them
finding-reproducer: F12 none — mutation testing: inside check-panel-citation-trigger.sh, scope a PATH prepend to a fake directory carrying a git shim that strips --end-of-options, immediately around the rev-parse --verify call only, then restore PATH before the COMMITTED/STAGED/UNSTAGED diff calls; rerun scripts/test-check-panel-citation-trigger.sh — full suite still passes because bash -x traces the command as parsed from source, not what the shimmed binary actually executed, so case 12's trace-based assertion is satisfied even though the flag never reached real git
finding-reproducer: F13 none — mutation testing: insert 'true; git() { command git "$@" | sed "s/--end-of-options //"; }; export -f git' right after set -euo pipefail in scripts/check-panel-citation-trigger.sh; rerun scripts/test-check-panel-citation-trigger.sh — all 24 cases report ok, including case 11b (F8), because the shadow regex is anchored ^[[:space:]]* and the definition is not the first statement on its line
finding-reproducer: F14 none — mutation testing: the awk insertion anchored on ^GIT_BIN= (the opening line of the multi-line GIT_BIN=... || { ... } statement) so the injected git() shadow line landed inside the || error branch and was never actually defined in the generated shadow copy; the mutation-proof case still reported ok for the wrong reason
finding-reproducer: F15 none — mutation testing: replace "$GIT_BIN" -C "$WORKTREE" with "git" -C "$WORKTREE" (bare git, double-quoted) at a real call site; before the fix, the negative BARE_GIT_CALLS regex does not match because the literal quote character immediately before git falls outside its excluded-character class, so the check reports 0 and passes; after the fix (a positive per-call-site check) it correctly fails
