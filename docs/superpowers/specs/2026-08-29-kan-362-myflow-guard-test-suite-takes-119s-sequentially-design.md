# KAN-362 — parallelise the guard test suite

The approved design for this change is its own artifact, and is canonical there rather than copied
here:

- `spectre/changes/kan-362-myflow-guard-test-suite-takes-119s-sequentially/design.md` — the
  measurements, the decisions, the testing and verification plans.
- `spectre/changes/kan-362-myflow-guard-test-suite-takes-119s-sequentially/proposal.md` — why the
  change exists and what it changes.
