# flow-settings — rationale

Reasoning behind `skills/flow-settings/SKILL.md`: the incident a rule was written against. Moved here
verbatim from the run-loaded file; **no `/flow-settings` run loads this file.** Each heading names
the source file and the section the passage came from.

## SKILL.md — 3. Write the change

The conductor-depth roster line printed ahead of the write:

> this line is the report KAN-398 asked for — the operator learns at
> save time, not mid-panel, that a configured slot has no agent type here —
