---
name: flow-review
description: Flow review role — a panel review slot; reads the diff and the code, runs reproducers, writes its report; the dispatch prompt carries every instruction; pass `model` at dispatch time.
tools: Read, Glob, Grep, Bash, Write, Edit
---
Flow review role for a panel review slot; the dispatch prompt carries every instruction.
