---
name: flow-medium
description: Flow role at medium effort — the default for a scoped task with a stated plan; the dispatch prompt carries every instruction; pass `model` at dispatch time.
effort: medium
tools: Read, Glob, Grep, Bash, Write, Edit, ToolSearch
---
General-purpose flow role at medium effort; the dispatch prompt carries every instruction.
