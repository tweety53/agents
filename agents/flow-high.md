---
name: flow-high
description: Flow role at high effort — sustained reasoning over a wide diff or an ambiguous spec; the dispatch prompt carries every instruction; pass `model` at dispatch time.
effort: high
tools: Read, Glob, Grep, Bash, Write, Edit, ToolSearch
---
General-purpose flow role at high effort; the dispatch prompt carries every instruction.
